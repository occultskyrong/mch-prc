# MCH-PRC 版本日志

中国近代史时间轴（1839–1949）项目 — 版本日志、进度记录与功能规划。

---

## 当前状态

| 指标 | 数量 |
|------|------|
| 总年份 | 110年 (1839-1949) |
| 总事件数 | 1101个 |
| 主事件数 | 1008个 (eventLevel=0) |
| 子事件数 | 93个 (eventLevel=1) |
| 人物数 | 480个 |
| 群体 | 9阵营 + 33子群体 |

---

## 版本日志

| 版本 | 日期 | 说明 |
|------|------|------|
| **v1.3.3** | — | 页面增加算法评分展示与筛选，更新评分算法逻辑 |
| **v1.3.2** | — | 更新可视化配色，方便定位事件和群体 |
| **v1.3.1** | 2026-04-22 | 子事件独立化架构重构、UI紧凑化、配色说明弹窗、docs重构 |
| v1.3.0 | 2026-04-18 | 矩阵视图动态加载、表头固定、筛选栏整合收起、MongoDB 后端驱动、史料来源独立注册表 |
| v1.2.0 | — | 事件影响因子量化评分系统、史料来源标注 |

---

## 进度记录

### 2026-04-21 完成（v1.3.1）

**子事件独立化架构重构**：
- 后端 Schema：`eventType` 从中文改为数字（1=战争, 2=条约, 3=起义, 4=改革, 5=事件），新增 `eventLevel`（0=主事件, 1=子事件）、`parentEventId` 自引用，删除嵌入式 `SubEvent[]`
- 后端 Service：`findAll` 支持 `eventLevel`/`parentEventId` 查询，`findOne` 附查子事件列表，`remove` 级联删除子事件
- 数据迁移：1101条事件全部转为数字 eventType，93条子事件拆为独立文档并计算影响力评分

**前端适配**：
- `EVENT_TYPE_COLORS`/`EVENT_TYPE_LABELS` 改为数字 key
- 新增 `showSubEvents` 状态控制子事件展示，子事件缩进+灰色样式区分
- 配色说明整合到 ? 按钮弹窗（Modal），移除底部图例栏
- 事件详情页新增"子事件"区块

**UI紧凑化优化**：
- 时间轴：筛选栏 padding 减半（8px→4px）、时期芯片 padding 减少、主内容区 padding 减少、群体/人物视图 gap 减半（24px→12px）
- 算法说明页：标题降级、卡片间距减半、添加 `size="small"`
- 事件详情页：外层 padding 减半（24px→12px）、卡片间距减半（16px→8px）
- 布局全局：表格单元格（4px 8px）、Card body（12px）、Typography/Collapse 全部压缩

**其他**：
- 启动命令统一：前后端均为 `npm run dev`
- 一次性脚本移至 `temp/` 目录，已加入 `.gitignore`
- docs 重构：3个因果链文档合并为 1 个，文档数从 8 精简为 4

### 2026-04-17 完成（v1.3.0）

1. **前端页面改进**

   - 时期展示：第一列显示时期名称+年份跨度（合并单元格），第二列显示年份
   - 时期选择器：顶部6个历史时期快捷选择
   - 事件标题：改用CSS截断而非JavaScript slice
   - 页面title改为"中国近代史时间轴 (1839-1949)"，favicon为SVG书卷图标

2. **数据文档改进**
   - raw/README.md 增加太平天国时期划分说明
   - 创建 CLAUDE.md 项目文档

3. **年份文件事件补充**
   - 1897、1899、1901、1937 年各从3个事件补充到10-11个
   - 1938-1945 年各从1-3个事件补充到10个（新增8个年份）

4. **数据转换**
   - 运行转换脚本生成 events.json
   - 当前总事件数：422个，子事件93个，人物304个

5. **影响因子评估系统（初始版）**
   - 设计影响因子数据结构：分数(0-100) + 描述
   - 创建自动化评估脚本 `scripts/add-impact-factor.cjs`
   - 为所有422个事件添加影响因子评估
   - 分布：高影响(90+): 141个，中等(50-89): 141个，低影响(0-49): 140个

---

## Feature 规划

### Feature 1: 事件影响因子量化评分系统

**目标**: 支持更强大、复杂的算法来计算历史事件的影响因子量化分数

**现状**:
- 现有影响因子分数为简单手动评估（0-100分）
- 缺乏科学的量化模型和多维度评估

**规划方案**:
1. **多维度评分模型**
   - 政治影响权重（政权更迭、制度变革）
   - 经济影响权重（财政、贸易、产业发展）
   - 社会影响权重（人口、民生、文化变迁）
   - 军事影响权重（战争规模、领土变化）
   - 国际影响权重（外交关系、条约签订）

2. **时间衰减因子**
   - 计算事件对后续历史的长远影响
   - 使用时间窗口评估即时影响 vs 持久影响

3. **连锁反应评估**
   - 识别事件的后续连锁事件
   - 计算因果链的影响传递系数

4. **AI辅助评估**
   - 使用LLM分析历史文献，生成影响评估报告
   - 基于历史学家的研究成果进行权重校准

### Feature 2: 基于RAG的事件关联发现

**目标**: 使用RAG（检索增强生成）技术找寻事件之间的关联关系

**现状**:
- 事件数据独立存储，缺乏关联分析
- 无法发现跨时期、跨群体的隐性关联

**规划方案**:
1. **知识库构建**
   - 将事件详细描述向量化存储
   - 建立人物、地点、时期的多维索引
   - 集成外部历史文献资料

2. **关联类型识别**
   - 因果关联（A事件直接导致B事件）
   - 并行关联（同期发生的相互影响事件）
   - 对比关联（相似性质的事件对比）
   - 人物关联（同一人物参与的多事件）

3. **RAG检索流程**
   - 向量相似度检索相关事件
   - LLM分析关联类型和强度
   - 生成关联解释文本

4. **关联数据存储**
   - 新增事件关联表（eventIdA, eventIdB, relationType, strength, explanation）
   - 支持关联关系的查询和可视化

### Feature 3: 历史事件关系图谱可视化

**目标**: 使用图算法和图可视化展示事件及关联人物之间的联系

**现状**:
- 现有展示为时间轴表格形式
- 无法直观展示复杂的人物-事件网络关系

**规划方案**:
1. **图数据模型**
   - 节点类型：事件、人物、群体、地点
   - 边类型：参与、因果、关联、对抗、合作
   - 边权重：关联强度、影响程度

2. **图算法应用**
   - 关键节点发现（PageRank识别核心事件/人物）
   - 社群发现（识别历史阵营和政治集团）
   - 路径分析（事件影响的传播路径）
   - 中心性分析（核心人物识别）

3. **可视化实现**
   - 使用 D3.js 或 AntV G6 实现交互式关系图谱
   - 支持节点点击查看详情、按时期/群体/人物筛选、图布局切换

4. **图谱页面**
   - 新增 `/graph` 路由
   - 左侧筛选面板、中央图谱展示区域、右侧详情面板

### 技术依赖

| Feature | 依赖 | 说明 |
|---------|------|------|
| F1 影响因子 | 无新增 | 可复用现有数据结构 |
| F2 RAG关联 | 向量数据库 | 使用 ai-foundation 的向量服务 |
| F3 图谱可视化 | D3.js 或 AntV G6 | 图可视化库 |

### 数据库扩展

| 表 | 新增字段/表 | 说明 |
|---|-------------|------|
| events | impactDimensions | 多维度影响评分 |
| - | event_relations | 事件关联关系表 |
| - | graph_nodes | 图节点缓存表 |
| - | graph_edges | 图边缓存表 |

### 实现优先级建议

1. **Phase 1**: Feature 3 图谱可视化（用户价值高，前端为主）
2. **Phase 2**: Feature 1 影响因子优化（数据质量提升）
3. **Phase 3**: Feature 2 RAG关联发现（需要AI服务集成）
